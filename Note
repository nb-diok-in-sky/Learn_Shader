
unity shader的结构
shader结构分为
properties subshader  以及pass

基本是在pass里面写shader主体代码
而会在Properties里面声明变量 并且初始化给予默认值，然后就在shader材质球上发现他 并且调节数值
比如 __—Float                          （“Float”                    ，         Float）          = 0.0
 
  shader内部调用变量名      真正在界面显示的名字                  变量真正的类型  默认值


同时也有range类  range（，）内部填上数字，外面就会显示一个可以拖拽的范围
	_Float("Float",Float) = 0.0
	_Range("Range",Range(0.0,1.0)) = 0.0
	_Vector("Vector",Vector)  =(1,1,1,1)
	_Color("Color",Color) = (0.5,0.5,0.5,0.5)
	_Texture("Texture",2D) = "black"{}

	
}常用的就这些


在Pass里面每次写东西之前要用CGPROGRAM和 ENDCG来包裹住
一些代码
在包裹层里面声明顶点着色器和片段着色器，以及获取的值
着色器会从这里面获取数据

浮点数里面也有变量使用的区分
float 32位 用于坐标
half  16位 用于UV\大部分向量
fixed 8位  用于颜色


CGPROGRAM
#pragma vertex vert
#pragma fragment frag
#include"UnityCG.cginc"

	struct appdata{
		float vertex: POSITION;//顶点坐标
		// float2 uv:TEXCOORD0;
		// float3 normal:NORMAL;
		// float4 color:COLOR;

	};

	strucr v2f{
		float4 pos:SV_POSITION;
	};

	v2f vert(appdata v){
		//这个方法就的意思是声明一个顶点着色器，在顶点着色器里面传入 appdata里面的数据
		//最后会返回 v2f里面的东西


	
	}
ENDCG





clamp方法 可以把数值限制在0到1之间 
直接用world normal法线点乘view dir 之后用clamp显示，再使用1 - 就是菲涅尔效果 可以理解为一个连续的数字的变化（从1到0）
step方法 传入 A和B值 如果B>A那就是返回一个1，否则返回0



当你在使用移动的贴图（做特效），并且物体可能需要频繁位移移动的时候，这时候你的贴图如果使用了世界坐标进行采样，那么你的贴图效果不会跟着你的模型走，会跟着世界空间走
你需要使用世界空间减去模型中心点（获取模型空间坐标也可以）把这个坐标拿去采样 这样你附着在人物上面的移动贴图就会以模型坐标来采样，跟着模型走
拿视图矩阵和世界法线相乘 就会使得法线跟随相机走，相机旋转法线也跟着旋转
使用step节点加上一个数 配合clip函数 可以通过调整数的大小来控制物体裁剪的范围
可以通过添加一张黑白渐变图片的u坐标搭配step or smoothstep效果来进行图像的剔除 
可以通过这个坐标来控制某一部分的剔除，具体取决于所选中的这个图片

使用一张渐变图片减去某个数值，就可以调控这张渐变图片的黑白图形范围占比，俗称把黑色白色范围改变  原理是 白色代表1 黑色代表0
把白色的部分减去了某个数就会变灰色，把灰色减某个数值就会变成黑色，黑色减数值，还是黑色
可以通过这种方法来调控
 然后通过distance方法  distance方法来获取图片中指定数的位置 会将对应位置的数的数值变为0，然后向周围逐渐增大为1
即 你选了个0.5 无论怎么改，distance方法过后，始终选定的是那个数在图片中的位置

frac函数可以只获取给定数的小数部分，可以配合time使用可以达到一个黑白图从黑到白，不断闪烁的功能，连上别的颜色就可以达成闪烁的感觉 

给uv查找数值的时候，有x，y两个项
x的值从0到一代表图片的从左到右，y的值代表图片的从上到下
比如  1,0.5就代表1这个点会采样图片的最右边的中间的颜色，其他的颜色会沿着0.5这一条线，也就是图片的中间线进行颜色的选取
其他的点也是，y轴代表这个图片的某一条横轴上的颜色，然后通过调整x的大小（0~1）来获取坐标的左右的大小

一般将物体顶部到底部的0-1的方法是使用World Normal 减去Transform Position （都用Y通道）这样就可以访问物体从上到下的一个位置
使用上面两个数值的xyz通道相减，再使用length求出长度 就可以获取轴心点中心到物体周围的数字从0-1的变化   可以拿来做从中心到两边的变化 （形成一个圆形范围）
可以考虑拿来做全息投影效果  也不一定说一定要减去Transform Position 的xyz值  你反正随便减去一个三维坐标并且进行求lenth的方法 你就可以获得一个  sphere mask的效果

当模型的边缘比较死板 比如说就是一条直线 一个方块的时候可以采取下面的方法 
思路是 正常的建模uv 越靠近零或者1 这个点就越接近边缘 uv方向都是这样子的啊 所以可以使用uv来找到模型的边缘 链接opacity的接口进行裁切 此时还是会比较生硬 可以自己乘以一下自己来将黑色 也就是0的范围扩大一些 此时黑色的部分会变得更加平滑（小于一的越承越小 所以黑色的地方就是0）
其他的地方的切边也差不多   比较蠢的办法是把uv的u通道 连出来两条 一条取负一条不变 拿去乘以入opacity接口的数字 很快啊 就是看起来很笨重很不聪明、
另外的方法就是将uv的u坐标拿去乘以2再减1 这样uv的坐标范围就是在-1到1的区间内。然后此时图片虽然是黑白对半劈但是有一边是-1 此时取了绝对值 这之后留下来会变成中间是黑色（因为是0） 而两边是白色（因为是1） 此时就把边缘变成了1 这时候再取拿1- 了 此时边缘就会变成0.中间变成1 







世界空间就是全局坐标系，所有的物体都会存放在这个世界空间里面
在写shader的时候遇见的世界坐标就是模型的这个顶点在世界坐标里面的位置
世界法线就是这个顶点的法线（模型的法线）
而世界光照是指当前世界的主光照方向 而不是那些光的方向 就是可以同时给予所有物体照明的一个平行光

而view dir可以用世界摄像机坐标的xyz通道减去 目前模型的世界坐标
即 _WorldSpaceCameraPos.xyz - i.pos_world;

给顶点坐标偏移加上法线方向然后对法线方向进行调整就会使得顶点沿着顶点法线位置进行偏移（沿着法线滑动放大缩小）
在导入法线贴图的时候 出现了很多噪点 可能是因为被导入法线的贴图提前做了卷积的工作 需要在被导入的贴图里的mipmode 来手动选择MipmapMod  里面的 mipm Level选项
这样在贴图的地方就会显示出采样的MipMap的层级，通过手动筛选MipMap层级的方式来消除
而计算Level层级的方法需要通过粗造度的G通道进行加一个自定义的-1——1的变量 用clamp限制后乘以0.7 再用1.7减去乘后的值 然后用减后的值拿去乘clamp值
最后拿去乘以一个6的倍数就会得到结果 然后通过自定义变量来调整层级


##距离函数Distance##
使用距离函数可以获取两个点之间的直线距离
拿着距离函数得到的结果减去某个常数，然后把这个结果送到颜色界面
此时可以通过调整这个常数的大小来控制颜色显示里面黑色和白色的占比 黑色为0，白色为大于等于1的地方
如果距离函数接了一个world position 另外一个随意 那么以那个点为中心 常量为半径会生出一个使物体数值绝对为0的圆
模型移动过去那个圆形范围就会变成黑色 数值为或者0一下	 
原理：通过距离函数来获取点到点的直线距离 这会使得物体上的点的数值一般都大于1
此时减去某个常数 当减的东西足够大的时候就可以让物体的颜色（数值变成0）变成黑色 可以拿来做效果
此时还是硬边缘，可以选择在控制范围大小的常速后去除以一个数字 这样就可以将边缘模糊化，数值越高模糊的范围越大
原理也很简单，依旧是 白色1 黑色0 除的数字越大越靠近0 所以也就越黑 白色的部分拿去除以了一个很大的数字 更大的数字除了变化比较小，而小的数字变化比较大
除以的数字和圈圈的范围呈现一个线性变化
包括在除法前面进行可以减去一个noise贴图 这个也可以使得边缘具有丰富的变化
之后对其进行美化的时候 可以新的append一个ramp渐变贴图 变换圆圈周围的边缘的颜色

在设置间隔的时候 可以设置一个随时间增长的变量 然后设置一个间隔数
之后设置if 当时间增长的数a 大于间隔数b 的时候 执行你要的方法   当然 记得在这个if前面加上限制 要不然这个就是一个无限循环的代码了







##每次写程序可以按照以下思路
做事情
声明你要做的方法
声明你做这个事情需要的载体（比如说从鼠标位置发射一枚射线）
声明做出这个动作的结果的信息、
触发动作后 将信息存储进载体的信息里面
将载体信息传入需要变动的物体





焚诀系列：

获取高光点或者高光周围的东西的焚诀 直接使用Power函数 EXP值越大 获取到的范围越小 越接近真正的高光点

简单纹理动画焚诀
Texcoord绑定noise贴图 uv通道链接panner  panner链接noise贴图    可选：因为没有alpha 所以将noise贴图r通道链接opacity 进行纹理裁剪 留下来黑白区域 此时把这个灰度区域拿去乘以原本的noise贴图纹理 就可以得到 边缘没那么锋利死板的边缘
关于一张noise贴图太简单的处理方式是 使用加法的形式 把 另外一张noise贴图的uv坐标加到原本图案的noise贴图uv里面 也就是在uv接口之前进行相加
如果做的是拖尾效果 此时可能会出现 头部该整的地方也被纹理扰动的很不整洁 尾部倒是还好 此时就可以使用纹理的u坐标或者v坐标来找到需要变换的那个头部 把u或者v的值拿去乘以原本负责扰动原有图像的noise贴图
此时就可以将图像的头部变得整齐一些了   原理是 因为u坐标 和v坐标都是因为一侧从0到1的值 即左边·到右边或下面到上面是是一个从黑到白（0-1）的一个渐变 拿黑色的部分去乘以需要变得整齐的部分 自然纹理感会变得整齐（减淡）


菲涅尔效果焚诀
先导入Normal Map 当然不导入也行 然后把法线贴图链接到 World Normal节点里面 拿着这个节点去点乘 view dir  之后使用 vlamp 来限制  之后用1减去 就会得到菲涅尔因子
法线贴图记得归一化

边缘光效果焚决
拿着边缘光效果 然后去可以通过power函数来控制边缘光的大小 用乘以 来控制边缘光的强度 然后用add来控制边缘光的偏移 记得用1- 这样可以反向 拿到的就是边缘光了


流光效果焚诀（从上到下扫描效果）
使用World Position 和Transform Position相减 然后使用Swizze节点获取他的xy分量 然后拿去接入panner位移就可以了，在panner之前可以接入一个四维向量 用他的xy分量和原效果相乘来调整偏移值  接入panner用zw分量来进行速度的调整（另外开一个2维向量也行 这只是物尽其用）
然后把panner接入uv贴图的uv通道 ok 大功告成
如果想进一步丰富 可以拿N dot V的数值拿去和World Position 和Transform Position相减后的swizze的xy分量相加（之前有声明过这些节点）  当然这时候可能 N dot V的数值范围在-1 到1的区间内 状态可能不是很好 会很奇怪 此时需要把他乘以0.5再加0.5让他调整到0-1的区间范围内
然后就有了一个纹理随身体流动的效果
这时候把他拿去乘以颜色 就得到了一个粗略的流光效果
这时候可以进一步进行深化 让他的纹理效果也有一个边缘处理 
可以继续拿一个NdotV 用1减去 NdotV 然后拿去乘以或者做加法来控制强度 之后拿去乘以刚刚的流光效果并且乘以一个颜色值 现在就有了一个流光的效果了

如果想要贴图随着形体贴在模型身上的话
需要拿一个Vector Position 传给 Transform 拿去减另外一个Transform Position （注意 两个transform position 都要勾选 To View 也就是要视觉空间）
然后拿着他们相减的结果的xy分量传输给你想要的贴图就行了



这是一个操作
之后如果想要做星光效果
可以拿着刚刚的xy分量加上一个数
具体是拿着法线贴图的世界坐标 把他的xy分量拿走  可以乘以一个变量 这个就是他的纹理的扭曲程度
相加之后就行了 此时纹理可以按照调整来进行扭曲

然后是星光效果 具体就是拿着一张动态的Noise贴图比如之前的流光效果的贴图 拿去和你想要制作的星空的贴图的相乘就行了 （最好是最后的值 因为有颜色 ） （法线贴图一定要是灰度图 不要有颜色）
然后可以用power节点对着你的星空贴图 拿去筛选高光（星星）点 详情见——获取高光点焚诀
原理是 一个动态的noise贴图 它上面会有不规则的黑色和白色和灰色 因为他是动态的 所以黑色和白色会覆盖到 不同的点 当白色覆盖到某个点的时候 这个点会变亮了 之后随着时间黑色或者灰色覆盖到这个点的时候 这个点会变暗
此时我们拿着刚刚获取到的高光图 拿去和那个noise贴图相乘 就可以得到星光闪烁的效果 甚至noise贴图也可以拿去power找到里面的白色的点 然后拿去和高光图相乘 得出的结果会使 亮点变黑变量更加激烈 也就是Bulin-Bulin的效果
这个效果是可以和之前的扭曲效果共存的 只要加起来就行了

	














